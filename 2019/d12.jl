println(pwd())
# f = open("1.in");
# input = map(i -> parse(Int64, i), readlines(f))
# close(f)

# function fuelForMass(mass)
#     massLeft = mass
#     fuel = 0
#     while(massLeft > 0)
#        fuel += massLeft ÷ 3 - 2
#        massLeft = fuel
#     end
#     fuel
# end

# fuelForMass(12)